# Optional. The app ships with seeded TypeScript data and works with these unset.
# When both are present, the home and detail pages read from the public `spots` table.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
